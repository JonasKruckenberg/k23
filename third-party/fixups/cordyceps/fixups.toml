# `[target.'cfg(loom)'.dependencies]` upstream. Reindeer resolves cfgs while
# generating, so the dependency rides the loom constraint instead.
[[extra_deps_select]]
"DEFAULT" = []
"constraints//:loom[enabled]" = ["//third-party:loom", "//third-party:tracing"]
