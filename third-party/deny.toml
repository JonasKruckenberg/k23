[graph]
targets = [
    "riscv64gc-unknown-none-elf",
    # You can also specify which target_features you promise are enabled for a
    # particular target. target_features are currently not validated against
    # the actual valid features supported by the target architecture.
    #{ triple = "wasm32-unknown-unknown", features = ["atomics"] },
]

[licenses]
allow = [
    "MIT", # requires license notice
    "Apache-2.0", # requires license notice
    "Unicode-3.0", # requires license notice
    "BSD-2-Clause", # requires license notice
    "Apache-2.0 WITH LLVM-exception",
    "MPL-2.0", # required by the ucs2 crate
    "Zlib",
    "NCSA"
]

[sources]
allow-git = [
    "https://github.com/bytecodealliance/wasmtime.git", # unreleased cranelift no_std work
]
