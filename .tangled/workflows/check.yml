when:
  - event: ["push", "pull_request"]
    branch: ["main"]
environment:
  CARGO_TERM_COLOR: always
  CLICOLOR: 1
  RUSTFLAGS: -Dwarnings
engine: "nixery"
steps:
  - name: cargo check
    command: nix develop --command just check
