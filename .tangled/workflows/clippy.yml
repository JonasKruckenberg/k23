when:
  - event: [ "push", "pull_request" ]
    branch: [ "main" ]

environment:
  CARGO_TERM_COLOR: always
  CLICOLOR: 1
  RUSTFLAGS: -Dwarnings

steps:
  - name: cargo clippy
    command: nix develop --command just clippy
