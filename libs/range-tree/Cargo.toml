[package]
name = "range-tree"
description = "SIMD optimized integer-range BTree"
version.workspace = true
edition.workspace = true
authors = ["Jonas Kruckenberg <iterpre@protonmail.com>"]
license.workspace = true

[lints]
workspace = true

[dependencies]
cfg-if = "1.0.4"
tracing.workspace = true

[dev-dependencies]
nonmax = { version = "0.5.5", default-features = false }
proptest = "1.9.0"
rand = { workspace = true, features = ["thread_rng"] }
tracing-subscriber.workspace = true

[[bench]]
name = "comparisons"
harness = false

[target.'cfg(not(target_os = "none"))'.dev-dependencies]
brie-tree = "0.1.2"
criterion = { workspace = true }                       #unified
pin-project.workspace = true
rand = { workspace = true, features = ["thread_rng"] }
wavltree.workspace = true
